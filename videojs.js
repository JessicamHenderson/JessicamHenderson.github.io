//fonts are actually specific too browser, so it might not show up correctly
	var g = document.getElementById("myCanvas");
var can = g.getContext("2d");
can.font = "40px Comic Sans MS";
can.fillText("GG",10,40);

	